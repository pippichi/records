还是结合之前的例子，假设进程1给进程2发送1、2、3、4这4个TCP段，如果主机B传输层已经顺利接收1、2这2个TCP段，那么主机B的TCP协议给主机A的TCP协议返回的TCP报文段中可以携带这样的信息：大写的ACK=1，小写的ack_seq=2500（在序号为2500的字节的前面所有字节都被顺利接收）。主机A的TCP协议收到这样的TCP报文段之后就可以确定1、2这2个TCP报文段已经被顺利接收



